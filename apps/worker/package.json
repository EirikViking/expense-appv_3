{
  "name": "@expense/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "deploy:review": "wrangler deploy --env review",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate:local": "wrangler d1 execute expense-db --local --file=../../migrations/001_initial.sql && wrangler d1 execute expense-db --local --file=../../migrations/002_analytics.sql && wrangler d1 execute expense-db --local --file=../../migrations/003_exclusions_and_transfers.sql && wrangler d1 execute expense-db --local --file=../../migrations/004_manual_ingested_files.sql",
    "db:migrate:remote": "wrangler d1 execute expense-db --remote --file=../../migrations/001_initial.sql && wrangler d1 execute expense-db --remote --file=../../migrations/002_analytics.sql && wrangler d1 execute expense-db --remote --file=../../migrations/003_exclusions_and_transfers.sql && wrangler d1 execute expense-db --remote --file=../../migrations/004_manual_ingested_files.sql"
  },
  "dependencies": {
    "@expense/shared": "workspace:*",
    "hono": "^4.6.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241018.0",
    "typescript": "^5.3.3",
    "vitest": "^2.1.0",
    "wrangler": "^3.114.16"
  }
}
